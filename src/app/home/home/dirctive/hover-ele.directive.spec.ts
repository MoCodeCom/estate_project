import { HoverEleDirective } from './hover-ele.directive';

describe('HoverEleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverEleDirective();
    expect(directive).toBeTruthy();
  });
});
